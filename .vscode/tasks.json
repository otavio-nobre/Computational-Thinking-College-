{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Rodar C",
      "type": "shell",
      "command": "gcc ${file} -o ${fileDirname}/${fileBasenameNoExtension}.exe; ${fileDirname}\\${fileBasenameNoExtension}.exe",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}